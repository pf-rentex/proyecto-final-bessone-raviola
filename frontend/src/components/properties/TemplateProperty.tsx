import CustomButton from '../commons/Button/CustomButton';
import { BiCalendar, BiTrash, TiEye, MdEdit, BiSearch } from 'react-icons/all';

const TemplateProperty = () => {
    return (
        <section className="bg-green-100 m-5 rounded cursor-pointer">
            <div className="flex flex-col container"></div>
        </section>
    );
};

export default TemplateProperty;
