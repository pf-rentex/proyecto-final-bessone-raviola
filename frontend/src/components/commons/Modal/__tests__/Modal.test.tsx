import React from "react";

// TODO: add tests
describe('Modal', () => {
    test('renders properly', () => {
        expect(1).toBe(1);
    });

    test('renders title', () => {
        expect(1).toBe(1);
    });

    test('renders content', () => {
        expect(1).toBe(1);
    });

    test('renders actions', () => {
        expect(1).toBe(1);
    });
});
